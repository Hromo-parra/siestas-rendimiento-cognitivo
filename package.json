{
  "name": "siestas-rendimiento-cognitivo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check db.js && node --check tasks.js && node --check protocol.js && node --check csv.js && node --check participar.js && node --check single-protocol.js && node --check single-storage.js && node --check single-tasks.js"
  }
}
